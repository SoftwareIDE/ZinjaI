#include <iostream>
#include <ctime>
using namespace std;

int main(int argc, char *argv[]) {
	char c[256];
	time_t t1 = time(0);
	tm *t2 = localtime( &t1 );
	strftime(c,256,"Dia: %A",t2);
	cout<<c<<endl;
	strftime(c,256,"Mes: %B",t2);
	cout<<c<<endl;
	strftime(c,256,"Hora: %H:%M:%S",t2);
	cout<<c<<endl;
	return 0;
}
