#include <iostream>
#include <ctime>
using namespace std;

int main(int argc, char *argv[]) {
	char c[256];
	time_t t1 = time(0);
	tm *t2 = localtime(t1);
	strftime(c,256,"",t);
	srand(time(0));
	time_t t1 = time(0);
	tm *t2 = localtime ( &t1 );
	cout<<"Hoy es: \n   "<<asctime(t2);
	t2->tm_mday+=100;
	t1 = mktime(t2);
	cout<<"Dentro de 100 dias sera: \n   "<<asctime(t2);
	return 0;
}
strftime

