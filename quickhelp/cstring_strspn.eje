#include <iostream>
#include <cstring>
using namespace std;

int main(int argc, char *argv[]) {
	char s[256];
	cout<<"Ingrese una cadena: ";
	cin.getline(s,256);
	int p = strspn(s,"012345689");
	if (s[p]=='\0') 
		cout<<"Solo hay numeros en esta cadena.\n";
	else
		cout<<"Hay un caracter no numerico en la posicion "<<p<<".\n";
	return 0;
}

